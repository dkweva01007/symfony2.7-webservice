
<!DOCTYPE html>

<html>
    <head>
        <title>Consultation du compte {{ user }}</title>
    </head>
    <body>
        <h1>Solde du compte {{ user }} :  {{ solde }}€ ! Date d'expiration : {{ limit_date }}</h1>
        <p>
            {{ form(form) }}
        </p>
        <table border='1'>
            <caption>Historique</caption>
            <tr>
                <th>site</th>
                <th>Montant</th>
                <th>Transaction</th>
                <th>Le</th>
            </tr>
            {% for historic in listhistoric %}
                <tr>
                    <td>{{ historic.getWebsiteId }}</td>
                    <td>{{ historic.getAmount }}</td>
                    <td>{{ historic.getActionType }}</td>
                    <td>{{ historic.getDate.format('d/m/Y H:i:s') }}</td>
                </tr>
            {% endfor %}
        </table>
    </body>
</html>